spring:
  datasource:
    url: jdbc:h2:mem:testdb-locadora
    username: sa
    password:

  h2:
    console:
      enabled: true
      path: /h2-console

  level:
    com.locadora_rdt_backend: INFO
    org.springframework: WARN
    org.hibernate: WARN
    org.apache: WARN